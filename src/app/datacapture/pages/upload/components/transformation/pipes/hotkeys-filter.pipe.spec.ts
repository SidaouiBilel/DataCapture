import { HotkeysFilterPipe } from './hotkeys-filter.pipe';

describe('HotkeysFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HotkeysFilterPipe();
    expect(pipe).toBeTruthy();
  });
});

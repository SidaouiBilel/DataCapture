import { WarningsExistPipe } from './warnings-exist.pipe';

describe('WarningsExistPipe', () => {
  it('create an instance', () => {
    const pipe = new WarningsExistPipe();
    expect(pipe).toBeTruthy();
  });
});

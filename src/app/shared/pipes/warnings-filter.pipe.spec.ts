import { WarningsFilterPipe } from './warnings-filter.pipe';

describe('WarningsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new WarningsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
